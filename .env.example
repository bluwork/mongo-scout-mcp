# MongoDB MCP Server Configuration

# MongoDB connection parameters
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=test

# Server mode configuration
# Options: 'read-only' (default, safe) or 'read-write' (enables all write operations)
# ⚠️  WARNING: 'read-write' mode allows AI assistants to modify, delete, or drop data
# ⚠️  Use 'read-write' only when explicitly needed and preferably with non-production databases
# SERVER_MODE=read-only

# Logging configuration
LOG_DIR=./logs
LOG_LEVEL=info  # Options: debug, info, warn, error

# Tool name prefix (for debugging prefixed tool calls)
# Leave empty for no prefix
TOOL_PREFIX=